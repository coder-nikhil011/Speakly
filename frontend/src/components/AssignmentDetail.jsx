import React, { useEffect, useState } from "react";
import { Link, useParams } from "react-router-dom";
import { getAssignment } from "../services/assignmentService";
export default function AssignmentDetail(){const {id}=useParams();const [item,setItem]=useState(null);const [loading,setLoading]=useState(true);useEffect(()=>{getAssignment(id).then(setItem).catch(()=>setItem(null)).finally(()=>setLoading(false));},[id]);if(loading)return <div className="min-h-screen flex items-center justify-center text-slate-400">Loading assignment…</div>;if(!item)return <div className="min-h-screen p-8 text-center"><h1 className="text-2xl font-bold">Assignment not found</h1><Link className="mt-4 inline-block underline" to="/assignments">Back to assignments</Link></div>;return <div className="min-h-screen bg-[#F8FAF9] p-6 sm:p-10"><div className="mx-auto max-w-4xl"><Link to="/assignments" className="text-sm font-bold text-slate-500">← Back to assignments</Link><div className="mt-8 rounded-[2rem] border border-slate-200 bg-white p-8"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-widest text-[#65B891]">TEACHER ASSIGNMENT</p><h1 className="mt-2 text-4xl font-extrabold">{item.title}</h1></div><span className="rounded-full bg-slate-100 px-4 py-2 text-xs font-bold">{item.status}</span></div><p className="mt-8 text-lg leading-8 text-slate-600">{item.description || "No instructions provided."}</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl bg-slate-50 p-5"><p className="text-xs font-bold uppercase tracking-widest text-slate-400">Students</p><p className="mt-2 text-2xl font-extrabold">{item.studentsEnrolled?.length || 0}</p></div><div className="rounded-2xl bg-slate-50 p-5"><p className="text-xs font-bold uppercase tracking-widest text-slate-400">Due</p><p className="mt-2 text-lg font-bold">{item.dueDate ? new Date(item.dueDate).toLocaleDateString() : "No due date"}</p></div></div></div></div></div>}
