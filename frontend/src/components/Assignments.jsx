import React, { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { getTeacherAssignments } from "../services/assignmentService";
export default function Assignments(){const [items,setItems]=useState([]);useEffect(()=>{getTeacherAssignments().then(setItems).catch(()=>setItems([]));},[]);return <div className="min-h-screen bg-[#F8FAF9] p-6 sm:p-10"><div className="mx-auto max-w-6xl"><div className="mt-12 flex items-center justify-between"><div><h1 className="text-4xl font-extrabold">Assignments</h1><p className="mt-2 text-sm text-slate-500">Real assignments sent to your connected students.</p></div><Link to="/create-assignment" className="rounded-xl bg-black px-5 py-3 text-sm font-bold text-white">+ Create</Link></div>{items.length===0?<div className="mt-8 rounded-3xl border border-dashed border-slate-200 bg-white p-10 text-center text-slate-500">No assignments yet. Create your first one.</div>:<div className="mt-8 grid gap-5 md:grid-cols-3">{items.map((item)=><Link key={item._id} to={`/assignment/${item._id}`} className="rounded-3xl border border-slate-200 bg-white p-7 transition hover:border-black"><div className="flex items-start justify-between gap-3"><h2 className="font-bold">{item.title}</h2><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{item.status}</span></div><p className="mt-3 text-sm text-slate-500">{item.studentsEnrolled?.length || 0} students</p><p className="mt-4 text-sm text-slate-600 line-clamp-3">{item.description || "No instructions added."}</p><p className="mt-6 text-sm font-bold underline">View assignment →</p></Link>)}</div>}</div></div>}
